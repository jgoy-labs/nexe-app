# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Node
node_modules
dist
dist-ssr
*.local

# Vite
.vite/

# Env (secrets i configs locals)
.env
.env.*
!.env.example

# Editor / IDE
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.claude/

# macOS
.DS_Store
._*

# Build outputs
target/
*.dmg
*.AppImage

# Tauri externalBin sidecar (regenerat per scripts/build-sidecar.sh)
src-tauri/binaries/
# Sidecar bundle tarball (regenerat per scripts/pre-bundle-sidecar.sh)
src-tauri/sidecar-bundle.tar.gz
# SHA-256 del tarball — generat al pre-bundle, llegit pel runtime
# (sidecar.rs) per detectar re-builds dins la mateixa CARGO_PKG_VERSION.
src-tauri/sidecar-bundle.sha256


# Python (si s'afegeix sidecar amb venv local)
__pycache__/
*.pyc
.venv/
venv/
.coverage
.coverage.*
htmlcov/

# Local dev notes and artifacts (not source)
notes/
*.notes.md
index_nexe-app.md
README-nexe-app.md

# Sidecar runtime dirs (regenerats al primer launch dev/Tauri)
storage/
personality/
src-tauri/storage/
src-tauri/personality/

# Scratch local de desenvolupament (no codi font)
dev-tools/

# Local tooling output and sync state (not source)
.muthur/
.gitoss-sync
